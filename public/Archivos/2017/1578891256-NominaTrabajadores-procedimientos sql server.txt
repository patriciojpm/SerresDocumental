 MessageBox.Show("si hay");
                            //string base64String;
                            //base64String = System.Convert.ToBase64String(streamhuella, 0, streamhuella.Length);
                            string url = "http://sistema.powerfitnesschile.cl/public/api/Registro/"+txtNombre+"/Csharp/";// + txtNombre.Text + "/" + Email.Text + "/" + Password.Text + "/" + base64String + "/C";
                            var json = new WebClient().DownloadString(url);
                            dynamic m = JsonConvert.DeserializeObject(json);

                            // api post
                            //HttpWebRequest request = WebRequest.Create("http://sistema.powerfitnesschile.cl/public/api/R/") as HttpWebRequest;
                            //request.Method = "POST";
                            //request.ContentType = "application/x-www-form-urlencoded";

                            //// Metodo modificado
                            //string postData = "username=miUsuraio&password=MiClave&grant_type=password&client_id=Miclient_id"; byte[] byteArray = Encoding.UTF8.GetBytes(postData);
                            //request.ContentLength = byteArray.Length;
                            //using (Stream dataStream = request.GetRequestStream())
                            //{
                            //    using (StreamWriter stmw = new StreamWriter(dataStream))
                            //    {
                            //        stmw.Write(postData);
                            //    }
                            //    dataStream.Write(byteArray, 0, byteArray.Length);
                            //}

                            //HttpWebResponse response = request.GetResponse() as HttpWebResponse;
                            //using (StreamReader reader = new StreamReader(response.GetResponseStream()))
                            //{
                            //    string resp = reader.ReadToEnd();
                            //    MessageBox.Show("valor devuelto" + resp);
                            //}

                            // fin api post


                            Usuario empleado = new Usuario()
                            {
                                //idWeb = (int)m,
                                Usuario1 = txtNombre.Text,
                                Huella = streamhuella,
                                Email = Email.Text,
                                Password = Password.Text,
                                //HuellaText = base64String;
                            };
                            contexto.Usuarios.Add(empleado);
                            contexto.SaveChanges();

                            MessageBox.Show("Usuario "+txtNombre.Text+" Asignado con Exito Id Registro"+m ); // + json
                            Listar();
                            txtNombre.Clear();
                            txtHuella.Clear();
                            Email.Clear();
                            Password.Clear();
                            Template = null;
                            btnAgregar.Enabled = false;
                        }